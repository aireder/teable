import React from 'react';
import ReactDOM from 'react-dom/client';
import { GridDemoApp } from './components/GridDemoApp';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <GridDemoApp />
  </React.StrictMode>
);